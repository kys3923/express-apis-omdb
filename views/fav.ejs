<h1>Favorite Movies</h1>

<% if (favMovieDB.length === 0) { %>
    <p>No results, <a href="/">Search again</a></p>
<% } else { %>
    <% favMovieDB.forEach(movie => { %>
       <h3><%= movie.title %></h3>
    <% }) %>
<% } %>